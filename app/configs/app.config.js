const config = {
  platform: process.platform,
  languages: ['en', 'de', 'ru', 'ja', 'zh-CN', 'ko', 'hi', 'kn', 'ml-IN', 'pa-IN', 'te'],
  fallbackLng: 'en',
  namespace: 'translation',
};

export default config;
